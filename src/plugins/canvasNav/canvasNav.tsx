import React, {useRef} from "react";
import "./canvasNav.scss"
import { Network } from "vis-network";


interface CanvasNavProps {
    network: Network
}

const CanvasNav = ({network}: CanvasNavProps) => {
    


    return <div className="canvasNav">
        Canvas Nav here
    </div>

}

export default CanvasNav