// src/DataStoreComponent.stories.js
import { createComponent } from './utils';

export default {
    title: 'DataStore/Events',
};

export const Default = () => {
    const container = createComponent();
    return container;
};